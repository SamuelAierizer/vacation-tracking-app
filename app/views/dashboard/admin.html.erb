<%= turbo_frame_tag :main_content do %>
  <h1 class="font-bold text-4xl mb-4">Admin</h1>

  <div class="flex flex-col gap-4 border-b last:border-b-0">
    <% @employees.each do |employee| %>
      <%= render DashboardCard.new(employee: employee, type: :admin) %>
    <% end %>
  </div>
<% end %>
